/**
 * Placeholder build script for v0.1.0 scaffold.
 * Extend when bundling renderer assets or compiling TypeScript.
 */
console.log("noteGen build: no bundling configured yet (Electron loads src/ directly).");
